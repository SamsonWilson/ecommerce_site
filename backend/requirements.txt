# Cœur
django~=5.1
djangorestframework
djangorestframework-simplejwt        # JWT
django-cors-headers                  # autoriser le domaine du frontend
django-environ                       # config par variables d'environnement
django-filter                        # filtres API (catégorie, couleur, prix)

# Base de données
psycopg[binary]                      # driver PostgreSQL

# Authentification Google (vérification de l'ID token)
google-auth
requests                             # transport HTTP requis par google-auth

# Stockage des médias (photos produits) sur S3 / Cloudflare R2 — activé par USE_S3
django-storages[s3]

# Production
gunicorn                             # serveur WSGI
whitenoise                           # fichiers statiques en prod
